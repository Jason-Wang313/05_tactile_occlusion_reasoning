# Hostile Reviewer Response

The strongest hostile review says the main comparison is unfair: dense entropy fails only because the simulator gives it irrelevant decorative variables. The v2 response is to agree and expose that boundary directly. Dense contact-only removes the decorative probes and reaches 100.0% success at budget 4, the same as contact-equivalence. Full dense entropy remains at 8.6% because it spends all four probes on decorative bits.

This means the paper should not claim a universal win over information gain. It should claim that dense hidden-state entropy is the wrong objective when visually hidden state contains variables that do not affect the pending manipulation contact. If a system already has an oracle contact-relevance filter, the representational gap can disappear.

The remaining weaknesses are still serious: no hardware, no realistic 3D geometry, idealized binary tactile outcomes, and no learned visuo-tactile policy comparison. These make the correct terminal venue decision workshop-only.
